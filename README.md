# Casa

A web application for handling home stuff.

## Deployment

The application is deployed on Railway, using the provided Dockerfile.
It relies on a SQLite database, which is provided as a volume attached to the web service.
